// Infigo streamer AirPlay configuration (shairport-sync)
// Used via the systemd drop-in: shairport-sync -c /etc/infigo/shairport-sync.conf
general =
{
  name = "Infigo Streamer";
};

alsa =
{
  // Card 0 is the USB DAC on Infigo streamers (onboard HDA is card 1)
  output_device = "hw:0";
};
